---
type: meta
title: "Wiki Lint 报告 2026-06-14"
tags:
  - lint
  - 质量保障
  - wiki维护
created: 2026-06-14
updated: 2026-06-14
status: draft
related: []
---

# Wiki Lint 报告 — 2026-06-14 首轮领域提炼

> 在首轮 Synthesis 操作中扫描全部 28 张 wiki 页面，记录质量问题和改进建议。

---

## 1. 孤儿页面

**标准**：零 inbound `wikilink`（无其他 wiki 页面引用）。

| 结论 | 说明 |
|------|------|
| ✅ 无孤儿 | 全部 28 张页面均有 ≥2 个 inbound wikilink。最少的是 `InfluxDB-指标设计与基数管理`（2 inbound）、`Doris-Nereids-CBO-优化器`（2 inbound）、`Event-Horizon-非对称依赖`（2 inbound — 来自 Doris-元数据与一致性复制、存储计算分离数据库的-Tail-Latency）。 |

---

## 2. Wikilink 格式错误

| 页面 | 位置 | 问题 | 建议修复 |
|------|------|------|----------|
| `Event-Horizon-非对称依赖.md` | frontmatter `related` | 引用 `[[事务模型深度调研]]` 带有错误的 `wiki/` 前缀 | 改为 `[[事务模型深度调研]]` |

**说明**：Obsidian 风格的 wikilink 直接使用文件名（不含路径前缀），`wiki/` 前缀会导致链接无法正确解析。

---

## 3. 矛盾描述检查

经全文交叉对比，**未发现实质矛盾**。以下为易被误判为矛盾但实际一致的 case：

| 疑似矛盾 | 页面 A | 页面 B | 实际一致性 |
|----------|--------|--------|-----------|
| Doris 4.0 Parquet 支持 | `Doris-Segment-v2`："4.x 计划支持 Parquet 原生格式" | `Doris-深度调研`："2025+ Doris 3.x~4.x: Parquet 原生支持" | ✅ 一致 — 3.x 已支持 Parquet 作为 Lakehouse Catalog 读取；4.x 计划作为原生存储格式 |
| Compaction 写放大后果 | `LSM-Tree-写放大`：写入吞吐被限制 | `Doris-Compaction-策略`：过度 Compaction 导致写放大 | ✅ 一致 — Doris 通过优先级控制和分级 Compaction 缓解了 LSM-Tree 的原生写放大问题 |
| InfluxDB v3 延迟 | `InfluxDB-3-列存引擎 `：毫秒级延迟 | `存储计算分离数据库的-Tail-Latency`：v3 存算分离存在 tail latency | ✅ 不一致而自洽 — v3 的"毫秒级"指写入后立即可查（单文件不等待 Compaction），tail latency 来自 Object Store 读取和存算分离的网络延迟 |

---

## 4. 页面状态统计

| 状态 | 数量 | 列表 |
|------|------|------|
| `final` | 10 | InfluxDB-深度调研及其 7 个子页、Doris-深度调研、事务模型深度调研 |
| `draft` | 18 | LSM-Tree 全部 7 页、Doris 7 个子页 (不含深度调研)、存算分离 4 页 |

**过时页面检查**：全部 28 张卡片创建于 2026-06-14（0 天前），无超过 30 天的 draft 页面。

**建议**：18 张 draft 页面应在 30 天内（2026-07-14 前）推进为 final。优先推进：
1. LSM-Tree-RUM猜想（理论基础页面）
2. LSM-Tree-写放大（瓶颈核心页面）
3. Doris-数据模型（与项目直接相关）

---

## 5. 概念缺口

在集群中发现以下概念被频繁引用，但缺乏独立的 wiki 页面：

| 概念 | 被引用次数 | 引用页面 | 当前覆盖方式 | 建议 |
|------|-----------|---------|-------------|------|
| **Bloom Filter** | 高频（LSM-Tree、InfluxDB、Doris 全部集群） | 散落在 ~15 个页面中 | 仅在 LSM-Tree 页面内简述 | 考虑创建独立的 Bloom Filter / 概率数据结构页面 |
| **WAL (Write-Ahead Log)** | 高频 | LSM-Tree、InfluxDB、Doris、事务模型 | 在 [[事务模型深度调研]] 中有覆盖 | 当前足够，事务模型页面已做了充分覆盖 |
| **Spanner / TrueTime** | 中频（事务模型、Event-Horizon） | 2 页 | 事务模型页面内有章节 | 当前足够 |
| **Percolator** | 中频（事务模型、Event-Horizon） | 2 页 | 事务模型页面内有章节 | 当前足够 |
| **MVCC** | 中高频 | LSM-Tree、事务模型、Doris | 事务模型页面内有章节 | 当前足够 |
| **Aurora 架构** | 低频（Tail-Latency） | 1 页 | 仅引用，无深入覆盖 | 低优先级，存算分离集群扩展时考虑 |
| **时序数据压缩算法** | 隐含（InfluxDB 提及 Snappy/ZSTD/Delta 编码） | 散落 | 无系统性覆盖 | 低优先级 |

**关键判断**：当前 28 张卡片的存量覆盖良好，不存在"无页面可查"的严重缺口。Bloom Filter 是最接近临界质量的概念——被 LSM-Tree、InfluxDB v3、Doris 三大集群同时引用，可能需要独立页面。

---

## 6. 集群临界质量评估

| 集群 | 页面数 | 通过条件 | 已生成 Synthesis |
|------|--------|---------|-----------------|
| LSM-Tree 存储引擎 | 7 | ≥5 页 ✅ | ✅ LSM-Tree-存储引擎体系综述 |
| InfluxDB 时序数据库 | 8 | ≥5 页 + ≥2 type + 多 final ✅ | ✅ InfluxDB-时序数据库体系综述 |
| Doris OLAP 数据库 | 8 | ≥5 页 + ≥2 type ✅ | ✅ Apache-Doris-OLAP-数据库体系综述 |
| 存算分离 / Tail Latency | 4 | ❌ 不满足 ≥5 / ≥2 type / final+3 | ❌ 未生成 |
| 事务模型 | 1 | ❌ 单页面 | ❌ 未生成 |

**存算分离集群说明**：4 个页面（RaaS、Tail-Latency、Log-as-the-Database、Event-Horizon）高度相关但未达临界质量。当集群扩展到 ≥5 页面或出现 1+ 个 final 状态且相关页面 ≥3 时，应触发 Synthesis。建议补充 Aurora/Socrates 架构、Kafka 存算分离等页面以促进集群成熟。

---

## 7. 跨集群连接强度

`事务模型深度调研` 是全场 **inbound 最多的页面**（13 个 inbound wikilink），作为连接 LSM-Tree、InfluxDB、Doris、分布式系统四大集群的枢纽——值得关注是否应提升为独立的 synthesis 综述页。

---

*报告生成时间：2026-06-14 · 首轮 Wiki Synthesize 操作*
