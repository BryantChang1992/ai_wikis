---
type: survey
title: "事务模型深度调研：从 ACID 到全球分布式事务"
sources:
  - "MySQL Internals Manual"
  - "PostgreSQL Documentation - Chapter 13. Concurrency Control"
  - "Google Percolator (OSDI 2010)"
  - "Spanner: Google's Globally-Distributed Database (OSDI 2012)"
  - "Calvin: Fast Distributed Transactions for Partitioned Database Systems (SIGMOD 2012)"
  - "TiDB 源码阅读 - 事务模型"
  - "CockroachDB Architecture - Transaction Layer"
tags:
  - 事务
  - 分布式系统
  - 数据库
  - 一致性
  - 并发控制
  - MVCC
  - 2PC
  - Percolator
  - Spanner
created: 2026-06-14
updated: 2026-06-14
status: final
author: Stark (CTO, CHANG_AI_TEAM)
related:
  - "[[LSM-Tree]]"
  - "[[Event-Horizon-非对称依赖]]"
---

# 事务模型深度调研：从 ACID 到全球分布式事务

> **作者**: Stark (CTO, CHANG_AI_TEAM)
> **日期**: 2026-06-14
> **版本**: v1.0
> **摘要**: 涵盖事务基础概念（ACID、隔离级别）、单机并发控制（2PL、MVCC）、分布式事务协议（2PC/3PC/TCC/SAGA）、现代事务架构（Percolator/Spanner/Calvin）以及主流数据库（MySQL/PostgreSQL/TiDB/CockroachDB）实现的全面技术调研。

---

## 目录

1. [事务基础概念](#1-事务基础概念)
2. [单机事务与并发控制](#2-单机事务与并发控制)
3. [分布式事务协议](#3-分布式事务协议)
4. [现代分布式事务架构](#4-现代分布式事务架构)
5. [核心系统实现对比](#5-核心系统实现对比)
6. [选型与实践建议](#6-选型与实践建议)

---

## 1. 事务基础概念

> **核心概念**: ACID | 隔离级别 | 事务

### 1.1 ACID 特性

事务（Transaction）是数据库操作的最小逻辑单元，具备四个基本特性：

| 特性 | 全称 | 含义 | 实现机制 |
|------|------|------|----------|
| **A** | Atomicity（原子性） | 事务所有操作要么全部成功，要么全部失败回滚 | Undo Log / Rollback |
| **C** | Consistency（一致性） | 事务执行前后，数据库从一致状态变换到另一个一致状态 | 约束检查 + AID 保障 |
| **I** | Isolation（隔离性） | 并发事务之间互不干扰，每个事务看到的数据视图一致 | 锁 / MVCC / 时间戳 |
| **D** | Durability（持久性） | 事务一旦提交，其结果永久保存，即使系统崩溃也不丢失 | Redo Log / WAL |

> **注意**: C（一致性）是 A、I、D 共同保证的最终结果，数据库本身通过约束（主键、外键、CHECK 等）辅助保障。

```python
# ACID 事务的典型代码模式
try:
    BEGIN
    debit(account_a, 100)     # 扣款
    credit(account_b, 100)    # 入账
    COMMIT
except:
    ROLLBACK  # 任何失败都回滚，保证 A
```

### 1.2 隔离级别与并发问题

SQL 标准定义了四种隔离级别，用于在并发控制开销与数据正确性之间做权衡：

| 隔离级别 | 脏读 (Dirty Read) | 不可重复读 (Non-Repeatable Read) | 幻读 (Phantom Read) |
|----------|:-----------------:|:-------------------------------:|:-------------------:|
| **Read Uncommitted** | ✅ 可能 | ✅ 可能 | ✅ 可能 |
| **Read Committed** | ❌ 禁止 | ✅ 可能 | ✅ 可能 |
| **Repeatable Read** | ❌ 禁止 | ❌ 禁止 | ✅ 可能（MySQL InnoDB 通过 Gap Lock 解决） |
| **Serializable** | ❌ 禁止 | ❌ 禁止 | ❌ 禁止 |

**三种典型并发问题**：

```
-- 脏读：事务 A 读到事务 B 未提交的数据
Time  T1: UPDATE accounts SET balance=500 WHERE id=1  -- 未提交
      T2: SELECT balance FROM accounts WHERE id=1      -- 读到 500
      T1: ROLLBACK                                     -- 实际回滚了

-- 不可重复读：同一事务内两次读到的值不同（其他事务 UPDATE 导致）
Time  T1: SELECT balance FROM accounts WHERE id=1      -- 读到 100
      T2: UPDATE accounts SET balance=200 WHERE id=1; COMMIT
      T1: SELECT balance FROM accounts WHERE id=1      -- 读到 200

-- 幻读：同一事务内两次查询的行数不同（其他事务 INSERT/DELETE 导致）
Time  T1: SELECT * FROM accounts WHERE balance > 100   -- 返回 5 行
      T2: INSERT INTO accounts VALUES (6, 150); COMMIT
      T1: SELECT * FROM accounts WHERE balance > 100   -- 返回 6 行
```

---

## 2. 单机事务与并发控制

> **核心概念**: 2PL | MVCC | WAL | ARIES

### 2.1 两阶段锁（2PL: Two-Phase Locking）

2PL 是经典的基于锁的并发控制协议。核心规则：

1. **Growing Phase（增长阶段）**: 只能获取锁，不能释放锁
2. **Shrinking Phase（收缩阶段）**: 只能释放锁，不能获取锁

```
时序图（Strict 2PL，实际使用最多的变体）:

获取锁阶段          释放锁阶段
   ↓                  ↓
  Lock(A)  Lock(B)    Unlock(A) Unlock(B)
    |--------|-----------|---------|
              ↑ 事务在此持有所有锁
```

**锁类型与兼容矩阵**：

|  | S (共享锁) | X (排他锁) |
|--|:---------:|:---------:|
| **S** | ✅ 兼容 | ❌ 冲突 |
| **X** | ❌ 冲突 | ❌ 冲突 |

**Strict 2PL**（严格两阶段锁）额外要求：排他锁必须持有到事务 COMMIT 之后才释放，保证了恢复时的可串行化。

**核心缺点**：
- **死锁风险**: 事务间互相等待对方持有的锁
- **读写互斥**: 读操作也需要获取共享锁，与写操作冲突
- **长事务阻塞**: 一个长事务会阻塞所有等待者

### 2.2 MVCC（Multi-Version Concurrency Control）

MVCC 通过保存数据的多个版本来实现读写互不阻塞，是当前主流数据库的标配。

**核心思想**：
- 写操作创建新版本，而不是原地修改
- 读操作根据事务快照读取对应版本
- 旧版本数据在不再需要时由垃圾回收（GC / Vacuum）清理

```
MVCC 版本链示意（PostgreSQL）:

元组头部（tuple header）存储:
  xmin: 创建该版本的事务 ID
  xmax: 删除/更新该版本的事务 ID

Row(id=1, value=100):
  v3: xmin=Tx5, xmax=NULL   ← 当前版本 (Tx5 改为 300)
  v2: xmin=Tx3, xmax=Tx5    ← Tx3 改为 200
  v1: xmin=Tx1, xmax=Tx3    ← Tx1 插入初始值 100

事务可见性判断（简化）:
  visible_if = (xmin < my_xid AND xmin IS COMMITTED)
               AND (xmax IS NULL OR xmax > my_xid OR xmax IS ROLLED BACK)
```

**MVCC 快照隔离（Snapshot Isolation）**：
- 每个事务在开始时获取一个快照时间戳
- 事务只能看到在它开始之前已提交的数据
- 实现了比 Serializable 弱但比 Read Committed 强的隔离

**核心优势**：
- 读不阻塞写，写不阻塞读
- 死锁概率大幅降低
- 对读多写少的 OLTP 场景友好

**核心代价**：
- 存储空间增大（多版本）
- 需要定期 GC/Vacuum 清理旧版本
- 长事务可能导致版本链过长

### 2.3 WAL（Write-Ahead Logging）预写日志

WAL 是保障持久性（Durability）和崩溃恢复的核心机制。

**规则**: 对数据页的修改必须先在日志中记录，日志落盘后才能修改数据页。


<svg viewBox="0 0 740 200" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="af1" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="currentColor"/></marker>
    <marker id="ad1" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <!-- Top row -->
  <rect x="10" y="10" width="130" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="75" y="30" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">User SQL</text>
  <text x="75" y="46" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">请求</text>
  <line x1="140" y1="35" x2="165" y2="35" stroke="currentColor" stroke-width="2" marker-end="url(#af1)"/>
  <rect x="170" y="10" width="130" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="235" y="30" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">WAL 日志</text>
  <text x="235" y="46" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">顺序写</text>
  <line x1="300" y1="35" x2="325" y2="35" stroke="currentColor" stroke-width="2" marker-end="url(#af1)"/>
  <rect x="330" y="10" width="130" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="395" y="30" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">内存 Buffer</text>
  <text x="395" y="46" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Pool</text>
  <line x1="460" y1="35" x2="485" y2="35" stroke="currentColor" stroke-width="2" marker-end="url(#af1)"/>
  <rect x="490" y="10" width="130" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="555" y="30" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">数据页</text>
  <text x="555" y="46" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">异步刷盘</text>
  <!-- Down arrow from WAL -->
  <line x1="235" y1="60" x2="235" y2="90" stroke="currentColor" stroke-width="2" marker-end="url(#ad1)"/>
  <!-- Bottom row -->
  <rect x="150" y="100" width="160" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="230" y="120" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">磁盘日志</text>
  <text x="230" y="138" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">← 持久化完成，事务可提交</text>
</svg>


**Redo Log vs Undo Log**:

| 日志类型 | 用途 | 记录内容 | 典型实现 |
|----------|------|----------|----------|
| **Redo Log** | 崩溃恢复（重放已完成但未刷盘的操作） | 物理/逻辑修改：`page_id=N, offset=O, old=A, new=B` | InnoDB Redo |
| **Undo Log** | 事务回滚 + MVCC 读历史版本 | 逆向操作：`将 row(id=1) 的 value 恢复为 100` | InnoDB Undo |

### 2.4 ARIES 崩溃恢复算法

ARIES（Algorithm for Recovery and Isolation Exploiting Semantics）是工业界最广泛采用的恢复算法，InnoDB、SQL Server 都基于它。

**三个核心原则**：

1. **WAL (Write-Ahead Logging)**: 先写日志再写数据
2. **Repeating History During Redo**: 恢复时重放所有操作（包括未提交事务的）
3. **Logging Changes During Undo**: 回滚未提交事务时也要记日志

**恢复三步走**：

```
Phase 1: Analysis（分析阶段）—— 从上一个 Checkpoint 开始扫描日志
  → 确定哪些事务已提交，哪些未提交
  → 构建脏页表（Dirty Page Table）和活跃事务表

Phase 2: Redo（重做阶段）—— 从最早的脏页 LSN 开始
  → 重放所有日志，恢复崩溃时丢失的数据页修改
  → 包括已提交和未提交的事务

Phase 3: Undo（回滚阶段）—— 从日志末尾向前扫描
  → 回滚所有未提交事务的操作
  → 对每个 Undo 操作也记录 CLR (Compensation Log Record)
```

**LSN (Log Sequence Number)** 是 ARIES 的心脏：
- 每个日志记录有全局递增的 LSN
- 每个数据页记录最后一次修改它的 LSN（pageLSN）
- Redo 时：`if log_record.LSN > page.pageLSN → 应用该修改`

---

## 3. 分布式事务协议

> **核心概念**: 2PC | 3PC | TCC | SAGA | 事务消息

当数据分布在多个节点上，单机事务机制不再够用，需要分布式事务协议。

### 3.1 2PC（Two-Phase Commit，两阶段提交）

2PC 是最经典的原子提交协议，引入**协调者（Coordinator）**角色。

```
Phase 1: Prepare（准备阶段）
  Coordinator → Participants: "Prepare?"
  参与者执行 WAL 写入，锁定资源，回复 "Yes" 或 "No"

Phase 2: Commit/Abort（提交/中止阶段）
  Coordinator: 若全员 "Yes" → "Commit"
              若有任一 "No" 或超时 → "Abort"
  Participants: 执行 Coordinator 的指令，释放锁
```

```
时序列（成功场景）:


<svg viewBox="0 0 680 200" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a2l" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="currentColor"/></marker>
    <marker id="a2r" markerWidth="8" markerHeight="6" refX="0" refY="3" orient="auto"><path d="M8,0 L0,3 L8,6 Z" fill="currentColor"/></marker>
  </defs>
  <!-- Participants -->
  <rect x="20" y="5" width="110" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="75" y="27" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Coordinator</text>
  <rect x="240" y="5" width="100" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="290" y="27" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Participant A</text>
  <rect x="440" y="5" width="100" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="490" y="27" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Participant B</text>
  <!-- Phase 1: Prepare -->
  <line x1="130" y1="42" x2="235" y2="42" stroke="currentColor" stroke-width="2" marker-end="url(#a2l)"/>
  <text x="180" y="38" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Prepare</text>
  <line x1="130" y1="58" x2="435" y2="58" stroke="currentColor" stroke-width="2" marker-end="url(#a2l)"/>
  <text x="280" y="54" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Prepare</text>
  <!-- Phase 2: Yes (Vote) -->
  <line x1="240" y1="74" x2="125" y2="74" stroke="currentColor" stroke-width="2" marker-end="url(#a2r)"/>
  <text x="180" y="70" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Yes</text>
  <line x1="440" y1="90" x2="125" y2="90" stroke="currentColor" stroke-width="2" marker-end="url(#a2r)"/>
  <text x="280" y="86" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Yes</text>
  <!-- Phase 3: Commit -->
  <line x1="130" y1="106" x2="235" y2="106" stroke="currentColor" stroke-width="2" marker-end="url(#a2l)"/>
  <text x="180" y="102" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Commit</text>
  <line x1="130" y1="122" x2="435" y2="122" stroke="currentColor" stroke-width="2" marker-end="url(#a2l)"/>
  <text x="280" y="118" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Commit</text>
  <!-- Phase 4: ACK -->
  <line x1="240" y1="138" x2="125" y2="138" stroke="currentColor" stroke-width="2" marker-end="url(#a2r)"/>
  <text x="180" y="134" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">ACK</text>
  <line x1="440" y1="154" x2="125" y2="154" stroke="currentColor" stroke-width="2" marker-end="url(#a2r)"/>
  <text x="280" y="150" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">ACK</text>
</svg>


**XA 规范**：X/Open 组织定义的 2PC 标准接口。大多数关系型数据库（MySQL、PostgreSQL、Oracle）都支持 XA。

```sql
-- XA 事务示例（MySQL）
XA START 'tx_001';
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
XA END 'tx_001';
XA PREPARE 'tx_001';   -- Phase 1: 进入 Prepared 状态

XA START 'tx_001';
UPDATE accounts SET balance = balance + 100 WHERE id = 2;
XA END 'tx_001';
XA PREPARE 'tx_002';

XA COMMIT 'tx_001';     -- Phase 2: 提交
XA COMMIT 'tx_002';
```

**优点**：
- 协议简单，实现标准统一
- 强一致性保障
- 大量数据库原生支持

**缺点**：
- **同步阻塞**: Prepare 后参与者必须等待 Coordinator 决策，期间资源锁定
- **单点故障**: Coordinator 崩溃导致整个系统阻塞
- **脑裂风险**: Coordinator 恢复后数据可能不一致（需要协调日志）
- **性能差**: 多次网络往返 + 同步写入

### 3.2 3PC（Three-Phase Commit，三阶段提交）

3PC 在 2PC 基础上增加一个阶段，并引入超时机制，试图解决阻塞问题：

```
Phase 1: CanCommit   → "你是否可以提交？"（资源检查，不锁定）
Phase 2: PreCommit   → "准备提交！"（锁资源，写 WAL）
Phase 3: DoCommit    → "执行提交！"
```

**关键改进**：
- 参与者可在 Coordinator 失联后超时自动提交（如果已进入 PreCommit）
- 减少同步阻塞的概率

**为什么 3PC 没有被广泛采用**：
- 只解决了**无分区**场景的阻塞，网络分区时仍然可能不一致
- 多一轮网络往返，延迟更高
- FLP 不可能性：在异步网络中无法完全解决 Coordinator 故障
- 2PC + Paxos/Raft（快速恢复 Coordinator）是更务实的方案

### 3.3 TCC（Try-Confirm-Cancel）

TCC 是**业务层的补偿事务**模式，将每个资源操作拆为三步：

```
Try（尝试）:     预留资源（检查+锁定），例如冻结余额
Confirm（确认）: 真正执行业务操作，例如扣款
Cancel（取消）:  释放已预留资源，例如解冻余额
```

```java
// TCC 转账示例（伪代码）
class TransferTcc {
    // Try 阶段
    boolean try() {
        fromAccount.freeze(100);  // 冻结 100 元
        toAccount.freeze(100);    // 预留 100 元额度
        return true;
    }

    // Confirm 阶段
    void confirm() {
        fromAccount.deductFrozen(100);  // 真实扣款
        toAccount.addBalance(100);      // 真实入账
    }

    // Cancel 阶段（try 失败时调用）
    void cancel() {
        fromAccount.unfreeze(100);      // 解冻
        toAccount.unfreeze(100);
    }
}
```

**与 2PC 的核心差异**：

| 维度 | 2PC | TCC |
|------|-----|-----|
| **层面** | 资源层（数据库协议） | 业务层（应用代码） |
| **锁定粒度** | 事务期间全程锁 | Try 后释放，Confirm 无需锁 |
| **性能** | 差（同步阻塞） | 好（非阻塞） |
| **实现成本** | 低（数据库原生） | 高（需要为每个接口写三套逻辑） |
| **一致性** | 强一致 | 最终一致 |

**适用场景**: 资金交易（冻结/解冻模式）、库存扣减（预占模式）、高并发业务。

### 3.4 SAGA 模式

SAGA（Saga）是处理长事务（Long-Lived Transaction, LLT）的模式，把一个大事务拆分为一系列有序的本地事务。

**两种协调方式**：


<svg viewBox="0 0 600 160" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a3" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="currentColor"/></marker>
    <marker id="a3d" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <rect x="10" y="10" width="110" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="65" y="32" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Service A</text>
  <line x1="120" y1="30" x2="155" y2="30" stroke="currentColor" stroke-width="2" marker-end="url(#a3)"/>
  <rect x="160" y="10" width="110" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="215" y="32" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Service B</text>
  <line x1="65" y1="50" x2="65" y2="75" stroke="currentColor" stroke-width="2" marker-end="url(#a3d)"/>
  <line x1="175" y1="50" x2="65" y2="75" stroke="currentColor" stroke-width="2"/>
  <rect x="10" y="80" width="110" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="65" y="102" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Service C</text>
  <line x1="120" y1="100" x2="155" y2="100" stroke="currentColor" stroke-width="2" marker-end="url(#a3)"/>
  <rect x="160" y="80" width="110" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="215" y="102" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Service D</text>
  <!-- Orchestrator side -->
  <rect x="400" y="10" width="130" height="30" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="465" y="27" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Orchestrator</text>
  <line x1="465" y1="40" x2="465" y2="55" stroke="currentColor" stroke-width="2"/>
  <line x1="465" y1="55" x2="380" y2="55" stroke="currentColor" stroke-width="2"/>
  <line x1="380" y1="55" x2="380" y2="75" stroke="currentColor" stroke-width="2"/>
  <line x1="380" y1="75" x2="405" y2="75" stroke="currentColor" stroke-width="2"/>
  <rect x="340" y="75" width="50" height="30" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="365" y="92" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">A</text>
  <rect x="420" y="75" width="50" height="30" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="445" y="92" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">B</text>
  <rect x="500" y="75" width="50" height="30" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="525" y="92" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">C</text>
  <text x="370" y="125" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">compensate / retry</text>
</svg>


**补偿机制核心**：
```
正向: T1 → T2 → T3 → ... → Tn
反向: Cn → ... → C3 → C2 → C1  (每个 Ti 有对应的补偿 Ci)

例如订单流程:
  T1: 创建订单      C1: 取消订单
  T2: 扣减库存      C2: 恢复库存
  T3: 扣款          C3: 退款
  T4: 更新积分      C4: 扣减积分
```

```python
# SAGA 编排器伪代码
class OrderSagaOrchestrator:
    def execute(self, order):
        steps = [
            ('create_order', 'cancel_order', lambda: order_service.create(order)),
            ('reserve_stock', 'release_stock', lambda: stock_service.reserve(order.items)),
            ('charge_payment', 'refund_payment', lambda: payment_service.charge(order.amount)),
        ]
        executed = []
        for name, compensate_fn, action in steps:
            try:
                result = action()
                executed.append((compensate_fn, result))
            except Exception:
                self._compensate(executed)  # 反向补偿
                raise
        return True

    def _compensate(self, executed):
        for compensate_fn, ctx in reversed(executed):
            compensate_fn(ctx)
```

**SAGA vs TCC**：

| 维度 | SAGA | TCC |
|------|------|-----|
| 事务长度 | 长事务（分钟~小时级） | 短事务（秒级） |
| 资源占用 | 无锁定，补偿式 | Try 阶段暂用资源 |
| 实现模式 | 编排/管控 | Try-Confirm-Cancel 三元组 |
| 隔离性 | 弱（事务间可看到中间状态） | 较好（Try 隔离业务资源） |

### 3.5 事务消息（Transactional Outbox）

以 RocketMQ 为代表的事务消息，保证本地事务与消息发送的原子性：


<svg viewBox="0 0 680 280" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a4" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="currentColor"/></marker>
    <marker id="a4d" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <text x="10" y="20" font-family="sans-serif" font-size="14" font-weight="bold" fill="currentColor">生产者流程:</text>
  <!-- Step 1 -->
  <rect x="10" y="30" width="160" height="36" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="90" y="50" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">1. 发送半消息</text>
  <line x1="170" y1="48" x2="240" y2="48" stroke="currentColor" stroke-width="2" marker-end="url(#a4)"/>
  <rect x="250" y="30" width="160" height="36" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="50" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Broker (消息不可见)</text>
  <!-- Arrow down -->
  <line x1="90" y1="66" x2="90" y2="85" stroke="currentColor" stroke-width="2" marker-end="url(#a4d)"/>
  <!-- Step 2 -->
  <rect x="10" y="90" width="160" height="36" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="90" y="110" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">2. 执行本地事务</text>
  <!-- Branch -->
  <line x1="90" y1="126" x2="90" y2="140" stroke="currentColor" stroke-width="2"/>
  <line x1="20" y1="140" x2="160" y2="140" stroke="currentColor" stroke-width="2"/>
  <line x1="20" y1="140" x2="20" y2="155" stroke="currentColor" stroke-width="2" marker-end="url(#a4d)"/>
  <line x1="160" y1="140" x2="160" y2="155" stroke="currentColor" stroke-width="2" marker-end="url(#a4d)"/>
  <rect x="-30" y="160" width="100" height="30" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="20" y="177" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Commit</text>
  <rect x="140" y="160" width="70" height="30" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="175" y="177" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Rollback</text>
  <line x1="20" y1="190" x2="20" y2="205" stroke="currentColor" stroke-width="2" marker-end="url(#a4d)"/>
  <line x1="175" y1="190" x2="175" y2="205" stroke="currentColor" stroke-width="2" marker-end="url(#a4d)"/>
  <text x="20" y="220" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">消息可见</text>
  <text x="175" y="220" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">消息删除</text>
  <!-- Broker 回查 -->
  <rect x="230" y="170" width="200" height="68" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="192" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">3. Broker 定期回查（未决事务）</text>
  <text x="330" y="210" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">→ 调用 Producer 的 check</text>
  <text x="330" y="226" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">→ 决定 Commit 或 Rollback</text>
</svg>


<svg viewBox="0 0 500 80" xmlns="http://www.w3.org/2000/svg">
  <rect x="10" y="10" width="440" height="60" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="230" y="30" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">3. Broker 定期回查（未决事务）</text>
  <text x="230" y="50" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">→ 调用 Producer 的 check  → 决定 Commit 或 Rollback</text>
</svg>
```

**本地消息表**模式（无事务消息支持时的变通）：

```sql
-- 在同一个本地事务中
BEGIN;
INSERT INTO orders (...) VALUES (...);           -- 业务操作
INSERT INTO outbox (topic, payload, status) ... -- 待发送消息
COMMIT;

-- 独立进程轮询 outbox 表，发送消息后标记已发送
SELECT * FROM outbox WHERE status = 'PENDING';
```

---

## 4. 现代分布式事务架构

> **核心概念**: Percolator | Spanner | TrueTime | Calvin

### 4.1 Percolator（乐观锁 + 时间戳 + 2PC）

Google Percolator（2010 OSDI）是一个建在 Bigtable 之上的增量处理系统，其事务模型被 TiDB/TiKV 借鉴并成为 NewSQL 领域的标杆方案。

**核心设计**：
- **无中心化事务管理器**: 客户端参与 2PC，去中心化
- **乐观并发控制**: 提交时才检测冲突
- **时间戳**: 基于 TSO（Timestamp Oracle）分配全局递增时间戳

**数据模型（Bigtable 列族）**：

```
Key: "row_key"
  Column "data"     → 当前值
  Column "lock"     → 分布式锁（primary key 指向）
  Column "write"    → commit 时间戳记录
```

**Percolator 事务流程**：

**Percolator 事务流程**：

```svg
<svg viewBox="0 0 720 310" xmlns="http://www.w3.org/2000/svg">
<text x="10" y="18" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="start" dominant-baseline="middle">Phase 1: Prewrite（预写）</text>
<rect x="10" y="28" width="700" height="100" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
<text x="25" y="46" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">1. 从 TSO 获取 start_ts</text>
<text x="25" y="64" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">2. 选一个 key 作为 primary，其余为 secondary</text>
<text x="25" y="82" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">3. 对所有 key 检查写写冲突:</text>
<text x="35" y="100" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">if 存在 write_record.ts &gt; start_ts → 冲突，abort</text>
<text x="25" y="118" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">4. 对 primary 写 lock 列</text>
<text x="25" y="136" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">5. 对所有 key 写 data 列（带 start_ts）</text>

<text x="10" y="172" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="start" dominant-baseline="middle">Phase 2: Commit</text>
<rect x="10" y="182" width="700" height="100" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
<text x="25" y="200" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">1. 从 TSO 获取 commit_ts</text>
<text x="25" y="218" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">2. 对 primary 写 write 列（commit_ts + start_ts）</text>
<text x="35" y="236" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">← 此操作成功标志事务提交</text>
<text x="25" y="254" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">3. 异步删除 primary lock</text>
<text x="25" y="272" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="start" dominant-baseline="middle">4. 异步清理 secondary 的 lock 和 write</text>
</svg>
```

**冲突检测与锁清理**：

```python
# Percolator 读操作伪代码
def read(key, start_ts):
    # 从最新版本向前扫描
    for version in range(latest, 0, -1):
        if version.write_ts <= start_ts:
            # 检查该版本对应的事务是否提交
            if version.write_ts >= version.start_ts:
                return version.data  # 已提交
        # 检查是否有锁
        if version.lock is not None:
            lock_ts = version.lock.start_ts
            if lock_ts < start_ts:
                # 这个锁是老事务留下的，检查 primary 状态
                primary_status = check_primary_lock(version.lock.primary)
                if primary_status == "COMMITTED":
                    cleanup_secondary_commit(key)  # 补提交
                else:
                    cleanup_secondary_rollback(key) # 回滚
    return None
```

**TiKV 的优化**：
- **并行 Prewrite**: 多个 secondary key 可以并行写入
- **Async Commit**: 允许不经过 TSO 的 commit_ts 获取（1PC 优化）
- **Pessimistic Lock**: 增加悲观锁模式，适应高冲突场景（TiDB 3.0+）

### 4.2 Spanner / TrueTime（全局分布式事务 + 原子钟）

Google Spanner（2012 OSDI）是全球首个实现**外部一致性（External Consistency）**的全球分布式数据库。

**核心创新：TrueTime API**

```
TrueTime 提供三个方法:
  TT.now()   → [earliest, latest]  一个时间区间，保证真实时间在这个区间内
                         ↑
           ε = (latest - earliest) / 2  ≈ 1-7ms（Google 数据中心内）
```

**外部一致性保证（Commit Wait）**：

```
对于读写事务：
  1. 获取所有锁
  2. 选择一个 commit timestamp s ≥ TT.now().latest
  3. 等待直到 TT.now().earliest ≥ s（Commit Wait）
  4. 释放锁，返回结果

  → 保证：若事务 T1 在 T2 开始前提交，
         则 T1.commit_ts < T2.commit_ts
```

**Spanner 架构要点**：


<svg viewBox="0 0 680 220" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a5" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="currentColor"/></marker>
    <marker id="a5d" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <rect x="200" y="10" width="240" height="36" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="320" y="30" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">TrueTime API ← GPS + Atomic Clocks</text>
  <line x1="320" y1="46" x2="320" y2="70" stroke="currentColor" stroke-width="2"/>
  <line x1="320" y1="70" x2="100" y2="70" stroke="currentColor" stroke-width="2"/>
  <line x1="320" y1="70" x2="540" y2="70" stroke="currentColor" stroke-width="2"/>
  <line x1="100" y1="70" x2="100" y2="90" stroke="currentColor" stroke-width="2" marker-end="url(#a5d)"/>
  <line x1="540" y1="70" x2="540" y2="90" stroke="currentColor" stroke-width="2" marker-end="url(#a5d)"/>
  <rect x="10" y="95" width="180" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="100" y="115" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Zone A (Paxos)</text>
  <text x="100" y="133" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Spanserver...</text>
  <rect x="450" y="95" width="180" height="50" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="540" y="115" font-family="sans-serif" font-size="13" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Zone B (Paxos)</text>
  <text x="540" y="133" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Spanserver...</text>
  <line x1="190" y1="130" x2="450" y2="130" stroke="currentColor" stroke-width="2" stroke-dasharray="6,3"/>
  <text x="320" y="125" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">2PC (跨 Paxos Group 事务)</text>
</svg>


**Spanner vs CockroachDB 的关键区别**：

| 维度 | Google Spanner | CockroachDB |
|------|---------------|-------------|
| 时钟 | TrueTime（原子钟硬件） | HLC（Hybrid Logical Clock） |
| 时钟不确定性 | ε ≈ 1-7ms | max_offset = 500ms（默认） |
| Commit Wait | 需要（2ε） | 不需要（Commit Wait 内置为不确定性等待） |
| 外部一致性 | 硬件保证 | 基于不确定性窗口保证 |
| 部署 | Google 专有 | 开源，任何环境 |

### 4.3 Calvin（确定性数据库）

Calvin（SIGMOD 2012）提出了一种**确定性执行**范式——在事务执行前就确定其全局顺序，从而消除传统 2PC 的协调开销。

**核心思想**：

```
传统数据库:                         Calvin:
  请求 → 锁/调度 → 执行 → 提交       请求 → 全局排序 → 确定性执行 → 提交
  (执行顺序决定提交顺序)             (提交顺序决定执行顺序)
```

**Calvin 架构流程**：


<svg viewBox="0 0 680 320" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a6" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <!-- Phase 1 -->
  <rect x="20" y="10" width="620" height="75" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="32" font-family="sans-serif" font-size="14" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Phase 1: Sequencing（排序层）</text>
  <text x="330" y="52" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">所有事务请求汇聚到 Sequencer，分配全局唯一顺序（批处理），广播到所有 Scheduler</text>
  <line x1="330" y1="85" x2="330" y2="105" stroke="currentColor" stroke-width="2" marker-end="url(#a6)"/>
  <!-- Phase 2 -->
  <rect x="20" y="110" width="620" height="75" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="132" font-family="sans-serif" font-size="14" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Phase 2: Scheduling（调度层）</text>
  <text x="330" y="152" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">每个分区独立按全局顺序执行事务。顺序确定 + 代码确定性 → 所有副本产生相同结果</text>
  <text x="330" y="170" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">无需 2PC！多分区事务直接以相同顺序在各分区执行</text>
  <line x1="330" y1="185" x2="330" y2="205" stroke="currentColor" stroke-width="2" marker-end="url(#a6)"/>
  <!-- Phase 3 -->
  <rect x="20" y="210" width="620" height="60" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="232" font-family="sans-serif" font-size="14" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Phase 3: Storage（存储层）</text>
  <text x="330" y="252" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">每个分区的存储节点持久化结果，确定性执行保证多副本一致</text>
</svg>


```

**为什么 Calvin 不需要 2PC**：
- 传统: 事务 T 涉及分区 A、B → 需要协调 A、B 同时提交
- Calvin: T 被编号为 #42 → A、B 各自独立看到 #42，执行 #42 → 自动一致

**Calvin 的局限性**：
- **必须提前知道所有读写集合**: 不支持交互式事务（一个查询结果决定下一个操作）
- **Sequencer 是瓶颈**: 虽然是可横向扩展的，但设计上所有事务要过排序层
- **延迟偏大**: 批处理 + 广播排序增加了基础延迟

---

## 5. 核心系统实现对比

> **核心概念**: MySQL | PostgreSQL | TiDB | CockroachDB

### 5.1 MySQL InnoDB

**架构概览**：


<svg viewBox="0 0 680 340" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a7" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <rect x="180" y="5" width="280" height="36" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="320" y="25" font-family="sans-serif" font-size="14" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Server Layer: SQL Parser → Optimizer</text>
  <line x1="320" y1="41" x2="320" y2="60" stroke="currentColor" stroke-width="2" marker-end="url(#a7)"/>
  <!-- InnoDB Engine outer box -->
  <rect x="140" y="65" width="360" height="260" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="320" y="82" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">InnoDB Engine</text>
  <!-- Buffer Pool -->
  <rect x="155" y="92" width="330" height="65" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5" stroke-dasharray="4,2"/>
  <text x="320" y="107" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Buffer Pool (内存)</text>
  <rect x="170" y="115" width="140" height="32" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="240" y="133" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Pages</text>
  <rect x="330" y="115" width="140" height="32" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="400" y="133" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Undo Log</text>
  <!-- Redo Log Buffer -->
  <line x1="320" y1="157" x2="320" y2="170" stroke="currentColor" stroke-width="1.5" marker-end="url(#a7)"/>
  <rect x="155" y="175" width="330" height="32" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5" stroke-dasharray="4,2"/>
  <text x="320" y="193" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Redo Log Buffer</text>
  <!-- ib_logfile -->
  <line x1="320" y1="207" x2="320" y2="225" stroke="currentColor" stroke-width="1.5" marker-end="url(#a7)"/>
  <rect x="180" y="230" width="280" height="36" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="320" y="250" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">ib_logfile (磁盘)</text>
</svg>


**MVCC 实现核心**：

InnoDB 通过 **Undo Log（回滚段）** 实现 MVCC：

```
每行数据隐藏列:
  DB_TRX_ID  : 最后一次修改该行的事务 ID (6 bytes)
  DB_ROLL_PTR: Undo Log 指针，指向回滚段记录 (7 bytes)
  DB_ROW_ID  : 行 ID（无主键时自动生成）

ReadView（读视图）判断可见性:
  m_ids[]     : 创建 ReadView 时活跃事务列表
  min_trx_id  : 活跃事务中的最小 ID
  max_trx_id  : 下一个将分配的事务 ID
  creator_trx_id: 创建该 ReadView 的事务 ID

可见性规则:
  if (trx_id == creator_trx_id) → 可见（自己的修改）
  if (trx_id < min_trx_id)      → 可见（已提交）
  if (trx_id >= max_trx_id)     → 不可见（未来事务）
  if (trx_id in m_ids[])        → 不可见（活跃未提交）
  else                          → 可见（已提交）
```

**隔离级别实现差异**：

| 隔离级别 | ReadView 生成时机 | 特殊机制 |
|----------|-------------------|----------|
| Read Uncommitted | 不使用 ReadView，直接读最新 | - |
| Read Committed | 每次 SELECT 创建新 ReadView | - |
| Repeatable Read | 事务中第一次 SELECT 时创建，后续复用 | Gap Lock 防止幻读 |
| Serializable | 同 RR + 所有 SELECT 隐式加 LOCK IN SHARE MODE | - |

**Redo Log 写入策略**（`innodb_flush_log_at_trx_commit`）：

| 值 | 策略 | 持久性 | 性能 |
|:--:|------|:------:|:----:|
| 0 | 每秒刷盘一次 Redo Log Buffer | ❌ 丢 1s 数据 | 最高 |
| 1 | 每次 COMMIT 都 fsync | ✅ 不丢数据 | 标准 |
| 2 | 每次 COMMIT 写 OS Cache，每秒刷盘 | ⚠️ OS 崩溃丢 | 较高 |

### 5.2 PostgreSQL

**MVCC 实现**：

PostgreSQL 不使用 Undo Log，而是把旧版本直接保留在堆表（Heap）中，通过 XID 和元组可见性规则判断：


<svg viewBox="0 0 520 120" xmlns="http://www.w3.org/2000/svg">
  <text x="10" y="20" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor">表元组（Heap Tuple）:</text>
  <rect x="10" y="30" width="500" height="80" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="25" y="50" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="start" dominant-baseline="middle">t_xmin: 插入/创建该元组的事务 ID</text>
  <text x="25" y="68" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="start" dominant-baseline="middle">t_xmax: 删除/更新该元组的事务 ID</text>
  <text x="25" y="86" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="start" dominant-baseline="middle">t_ctid: 指向新版本的指针（更新时） · t_infomask: 标志位</text>
</svg>


**XID 回卷（Wraparound）问题**：

PostgreSQL 使用 32-bit 事务 ID，约 43 亿个。当 XID 回卷时，需要特殊处理：

```
XID 空间逻辑视图（环形）:


<svg viewBox="0 0 680 150" xmlns="http://www.w3.org/2000/svg">
  <text x="10" y="18" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor">XID 空间逻辑视图（环形）:</text>
  <!-- Header: 2^31-1 label -->
  <text x="340" y="50" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">2^31-1 ↓</text>
  <!-- Regions -->
  <rect x="10" y="60" width="160" height="36" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="90" y="80" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">... 过去</text>
  <rect x="175" y="60" width="100" height="36" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="225" y="80" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Frozen</text>
  <rect x="280" y="60" width="60" height="36" rx="4" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="310" y="80" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Cur</text>
  <rect x="350" y="60" width="130" height="36" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="415" y="80" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">未来...</text>
  <!-- Arrow -->
  <line x1="495" y1="78" x2="530" y2="78" stroke="currentColor" stroke-width="2" marker-end="url(#a4)"/>
  <rect x="535" y="60" width="90" height="36" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="580" y="80" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Wraparound</text>
  <!-- Labels -->
  <text x="310" y="105" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">↑ 0 (回卷点)</text>
</svg>


Frozen（冻结）: xmin 设为 FrozenTransactionId (2)
  该元组对所有事务可见，不需要再判断 XID 比较

VACUUM FREEZE 必须定期执行，防止 XID 耗尽变成 wraparound
→ 如果达到 age(datfrozenxid) > 2 亿，触发 aggressive VACUUM
→ 如果达到 20 亿，数据库进入只读模式
```

### 5.3 TiDB / TiKV

**架构分层**：


<svg viewBox="0 0 680 320" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a10" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <rect x="180" y="5" width="280" height="40" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="320" y="27" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">TiDB (SQL 层): 解析 → 优化 → 执行计划</text>
  <line x1="320" y1="45" x2="320" y2="65" stroke="currentColor" stroke-width="2" marker-end="url(#a10)"/>
  <!-- TiKV outer box -->
  <rect x="100" y="70" width="440" height="240" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="320" y="88" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">TiKV (存储 + 事务)</text>
  <!-- Raft -->
  <rect x="120" y="98" width="400" height="32" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="320" y="116" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Raft 层: 多副本一致性</text>
  <!-- Percolator -->
  <rect x="120" y="140" width="400" height="55" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5" stroke-dasharray="4,2"/>
  <text x="320" y="155" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Percolator 事务层</text>
  <rect x="140" y="162" width="170" height="24" rx="3" fill="transparent" stroke="currentColor" stroke-width="1"/>
  <text x="225" y="176" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">MVCC</text>
  <rect x="330" y="162" width="170" height="24" rx="3" fill="transparent" stroke="currentColor" stroke-width="1"/>
  <text x="415" y="176" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">2PC (客户端)</text>
  <!-- RocksDB -->
  <rect x="120" y="205" width="400" height="32" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="320" y="223" font-family="sans-serif" font-size="12" fill="currentColor" text-anchor="middle" dominant-baseline="middle">RocksDB (单机 KV 引擎)</text>
</svg>


**关键设计决策**：

| 设计点 | 选择 | 原因 |
|--------|------|------|
| 事务模型 | Percolator（乐观 2PC） | 无中心化，可水平扩展 |
| 时间戳 | PD（Placement Driver）TSO | 全局递增时间戳，每节点 40w+ QPS |
| 冲突处理 | 乐观（默认）/ 悲观（可选） | 适配不同冲突率场景 |
| GC | TiKV GC Worker | 定期清理过期 MVCC 版本 |
| 1PC 优化 | Async Commit | 单行事务免除 TSO 等待 |

### 5.4 CockroachDB

**事务模型**：

CockroachDB 的事务模型基于 **并行提交（Parallel Commit）+ 写入意向（Write Intent）+ 时间戳缓存**。

```
事务执行流程:


<svg viewBox="0 0 680 400" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a11" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
  </defs>
  <text x="10" y="18" font-family="sans-serif" font-size="14" font-weight="bold" fill="currentColor">事务执行流程:</text>
  <!-- Step 1 -->
  <rect x="10" y="30" width="640" height="70" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="50" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">1. 写阶段</text>
  <text x="330" y="68" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">写入 Write Intent（带 provisional value），获取 Transaction Record，事务状态: PENDING</text>
  <line x1="330" y1="100" x2="330" y2="115" stroke="currentColor" stroke-width="2" marker-end="url(#a11)"/>
  <!-- Step 2 -->
  <rect x="10" y="120" width="640" height="70" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="140" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">2. 提交阶段 (Parallel Commit)</text>
  <text x="330" y="158" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Transaction Record → COMMITTED，参与者并行确认，异步清理 Write Intents</text>
  <line x1="330" y1="190" x2="330" y2="205" stroke="currentColor" stroke-width="2" marker-end="url(#a11)"/>
  <!-- Step 3 -->
  <rect x="10" y="210" width="640" height="70" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="230" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">3. 读路径</text>
  <text x="330" y="248" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Write Intent → Transaction Record: COMMITTED=读取 / PENDING=等待 / ABORTED=忽略</text>
  <line x1="330" y1="280" x2="330" y2="295" stroke="currentColor" stroke-width="2" marker-end="url(#a11)"/>
  <!-- Step 4 -->
  <rect x="10" y="300" width="640" height="55" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="318" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">4. 时钟不确定性处理 (HLC)</text>
  <text x="330" y="336" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Global Uncertainty Limit = max_offset，commit_ts 落在不确定性窗口内时等待</text>
</svg>


2. 提交阶段 (Parallel Commit):
```svg
<svg viewBox="0 0 600 90" xmlns="http://www.w3.org/2000/svg">
<rect x="10" y="10" width="580" height="70" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
<text x="300" y="30" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">提交阶段 (Parallel Commit):</text>
<text x="25" y="50" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="start" dominant-baseline="middle">将 Transaction Record 状态改为 COMMITTED</text>
<text x="25" y="66" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="start" dominant-baseline="middle">参与者并行确认 → 只需等待最慢的写入 | 异步清理 Write Intents</text>
</svg>

```

3. 读路径:
```svg
<svg viewBox="0 0 600 90" xmlns="http://www.w3.org/2000/svg">
<rect x="10" y="10" width="580" height="70" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
<text x="300" y="30" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">读路径:</text>
<text x="25" y="50" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="start" dominant-baseline="middle">遇到 Write Intent → 查找 Transaction Record</text>
<text x="25" y="68" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="start" dominant-baseline="middle">COMMITTED → 读取该值 | PENDING → 等待或推入 STAGING | ABORTED → 忽略，读旧版本</text>
</svg>

```

4. 时钟不确定性处理 (HLC):
```svg
<svg viewBox="0 0 600 70" xmlns="http://www.w3.org/2000/svg">
<rect x="10" y="10" width="580" height="50" rx="4" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
<text x="300" y="28" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">时钟不确定性处理 (HLC):</text>
<text x="25" y="48" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="start" dominant-baseline="middle">Global Uncertainty Limit = max_offset，若 commit_ts 落在不确定性窗口内则等待</text>
</svg>

```
### 5.5 系统综合对比

| 维度 | MySQL InnoDB | PostgreSQL | TiDB/TiKV | CockroachDB | Spanner |
|------|:-----------:|:----------:|:---------:|:-----------:|:-------:|
| **事务模型** | 2PL + MVCC | MVCC (堆内多版本) | Percolator (乐观) | Parallel Commit | TrueTime 2PC |
| **隔离级别** | RC / RR (默认) | RC / RR / Serial | SI (默认) / RC | Serializable (默认) | Serializable |
| **分布式** | 单机为主 | 单机为主 | 原生分布式 | 原生分布式 | 原生分布式 |
| **时钟** | 单机 | 单机 | PD TSO | HLC | TrueTime (原子钟) |
| **锁机制** | 行锁 + Gap Lock | 行锁 + 谓词锁 | 乐观 + 悲观(可选) | 无锁(Write Intent) | 悲观锁 |
| **MVCC GC** | Purge Thread | VACUUM | GC Worker | MVCC GC Queue | Compaction |
| **CAP** | CA | CA | CP (默认) | CP | CP (强 C) |
| **存储引擎** | B+ Tree | Heap | RocksDB (LSM) | Pebble (LSM) | Colossus (GFS2) |
| **XA 支持** | ✅ | ✅ | ❌ (用其他方式) | ❌ (用其他方式) | ✅ |
| **开源** | ✅ | ✅ | ✅ | ✅ (BSL) | ❌ (Google Cloud) |

---

## 6. 选型与实践建议

> **相关**: 事务模型选择决策树 | 分布式事务最佳实践

### 6.1 事务模型选择决策树


<svg viewBox="0 0 680 420" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <marker id="a12" markerWidth="6" markerHeight="8" refX="3" refY="8" orient="auto"><path d="M0,0 L3,8 L6,0 Z" fill="currentColor"/></marker>
    <marker id="a12r" markerWidth="8" markerHeight="6" refX="8" refY="3" orient="auto"><path d="M0,0 L8,3 L0,6 Z" fill="currentColor"/></marker>
  </defs>
  <rect x="210" y="5" width="240" height="36" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="330" y="25" font-family="sans-serif" font-size="13" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">需要分布式事务？</text>
  <!-- No branch -->
  <line x1="250" y1="41" x2="250" y2="60" stroke="currentColor" stroke-width="2"/>
  <line x1="120" y1="60" x2="380" y2="60" stroke="currentColor" stroke-width="2"/>
  <line x1="120" y1="60" x2="120" y2="75" stroke="currentColor" stroke-width="2" marker-end="url(#a12)"/>
  <line x1="380" y1="60" x2="380" y2="75" stroke="currentColor" stroke-width="2" marker-end="url(#a12)"/>
  <text x="250" y="55" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">No</text>
  <text x="500" y="55" font-family="sans-serif" font-size="11" fill="currentColor" text-anchor="middle" dominant-baseline="middle">Yes →</text>
  <!-- No: single machine -->
  <rect x="20" y="80" width="200" height="80" rx="6" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="120" y="100" font-family="sans-serif" font-size="12" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">单机数据库？</text>
  <text x="120" y="118" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">OLTP → MySQL/PostgreSQL</text>
  <text x="120" y="134" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">OLAP → ClickHouse/Doris</text>
  <text x="120" y="150" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">嵌入式 → SQLite</text>
  <!-- Yes: consistency needs -->
  <rect x="400" y="85" width="250" height="55" rx="6" fill="transparent" stroke="currentColor" stroke-width="2"/>
  <text x="525" y="105" font-family="sans-serif" font-size="12" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">一致性需求？</text>
  <line x1="475" y1="140" x2="475" y2="155" stroke="currentColor" stroke-width="1.5" marker-end="url(#a12)"/>
  <line x1="575" y1="140" x2="575" y2="155" stroke="currentColor" stroke-width="1.5" marker-end="url(#a12)"/>
  <!-- Strong -->
  <rect x="380" y="160" width="190" height="80" rx="6" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="475" y="180" font-family="sans-serif" font-size="12" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">强一致 (Serializable)</text>
  <text x="475" y="198" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">全球部署 → Spanner</text>
  <text x="475" y="214" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">确定性执行 → Calvin 系</text>
  <text x="475" y="230" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">通用 → CockroachDB</text>
  <!-- SI -->
  <rect x="480" y="255" width="190" height="80" rx="6" fill="transparent" stroke="currentColor" stroke-width="1.5"/>
  <text x="575" y="275" font-family="sans-serif" font-size="12" font-weight="bold" fill="currentColor" text-anchor="middle" dominant-baseline="middle">快照隔离 (SI / RR)</text>
  <text x="575" y="293" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">MySQL 生态 → TiDB</text>
  <text x="575" y="309" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">分布式 → CockroachDB SI</text>
  <text x="575" y="325" font-family="sans-serif" font-size="10" fill="currentColor" text-anchor="middle" dominant-baseline="middle">NoSQL → Fluss</text>
</svg>


### 6.2 隔离级别 vs 性能——量化参考

以下数据来自公开基准测试（sysbench / YCSB，仅供参考）：

| 隔离级别 | 吞吐量（相对） | 锁竞争率 | 适用场景 |
|----------|:------------:|:--------:|----------|
| Read Uncommitted | 100% | 无 | 仅限日志/监控等脏读可接受场景 |
| Read Committed | 95% | 低 | 通用 OLTP，大多数互联网应用 |
| Repeatable Read (MySQL) | 90% | 中 | 需要事务内一致性读的业务 |
| Serializable (2PL) | 30-50% | 高 | 强一致性需求的金融核心 |
| Serializable (SSI) | 70-85% | 中低 | PostgreSQL/CockroachDB 的可串行化快照隔离 |

### 6.3 分布式事务最佳实践

#### 6.3.1 避免滥用分布式事务

```
❌ 反模式:
  将多个微服务通过 2PC XA 编排
  → 锁持有时间长，Coordinator 单点，性能灾难

✅ 正确姿势:
  1. 先问：能不能通过设计避免跨库事务？
  2. 若能保证最终一致：用本地消息表 / 事务消息
  3. 若必须强一致：用支持原生分布式事务的 NewSQL
  4. 只在实在没办法时才考虑 XA 2PC
```

#### 6.3.2 事务超时与自动回滚

```sql
-- MySQL 事务超时设置
SET SESSION innodb_lock_wait_timeout = 5;        -- 锁等待超时 5 秒
SET SESSION innodb_rollback_on_timeout = ON;      -- 超时自动回滚整个事务

-- PostgreSQL
SET statement_timeout = '10s';                    -- 单条 SQL 超时
SET idle_in_transaction_session_timeout = '30s';  -- 空闲事务超时
```

#### 6.3.3 Percolator 系统的冲突优化

```python
# TiDB/TiKV 层面的优化策略

# 1. 热点避免：主键设计
# ❌  AUTO_INCREMENT 主键 → 所有插入落在同一个 Region
# ✅  SHARD_ROW_ID_BITS 或 UUID 打散

# 2. 大事务拆分
# ❌ 单事务 1000w 行 → 锁冲突 + OOM
# ✅ 分批处理，每批独立事务

# 3. 悲观锁模式（高冲突场景）
# TiDB 配置
SET @@tidb_txn_mode = "pessimistic";

# 4. GC 安全点控制（避免长事务阻塞 GC）
SHOW VARIABLES LIKE 'tikv_gc_life_time';  -- 默认 10min
```

#### 6.3.4 TCC 实现中的幂等性

```java
// TCC 的 Confirm/Cancel 必须幂等
// 典型做法：状态机 + 唯一键

public class TransferTccService {
    // Try
    public void tryFreeze(String tccId, String accountId, BigDecimal amount) {
        // 幂等检查
        if (freezeRecordRepo.existsByTccId(tccId)) return;
        // 乐观锁扣减
        int rows = accountRepo.freezeBalance(accountId, amount);
        if (rows == 0) throw new InsufficientBalanceException();
        freezeRecordRepo.insert(new FreezeRecord(tccId, "TRYING"));
    }

    // Confirm
    public void confirm(String tccId) {
        FreezeRecord record = freezeRecordRepo.findByTccId(tccId);
        if (record.getStatus() == "CONFIRMED") return;  // 幂等
        accountRepo.confirmDeduct(record);
        freezeRecordRepo.updateStatus(tccId, "CONFIRMED");
    }
}
```

#### 6.3.5 时钟相关注意事项

| 系统 | 时钟依赖 | 风险 | 缓解 |
|------|----------|------|------|
| TiDB | PD TSO | PD 故障→无法分配时间戳 | PD 集群 (3/5 节点) |
| CockroachDB | HLC (NTP) | 时钟偏移 > max_offset → 异常 | 配置保守的 max_offset，NTP 监控 |
| Spanner | TrueTime (原子钟+GPS) | 硬件故障 | 冗余原子钟 + GPS 天线 |
| 2PC (传统) | 无特殊依赖 | N/A | N/A |

#### 6.3.6 分布式事务监控告警指标

```
关键指标:
  □ 事务延迟 (P50 / P99 / P999)
  □ 事务提交失败率（冲突率）
  □ 事务重试次数
  □ 正在执行的活跃事务数
  □ 锁等待队列长度
  □ TSO 分配延迟（TiDB）
  □ GC 安全点停留时间（长事务阻塞 GC）
  □ MVCC 版本数（版本膨胀）
```

---

## 附录 A: 术语对照表

| 缩写 | 全称 | 中文 |
|------|------|------|
| ACID | Atomicity, Consistency, Isolation, Durability | 原子性、一致性、隔离性、持久性 |
| MVCC | Multi-Version Concurrency Control | 多版本并发控制 |
| 2PL | Two-Phase Locking | 两阶段锁定 |
| WAL | Write-Ahead Logging | 预写日志 |
| ARIES | Algorithm for Recovery and Isolation Exploiting Semantics | 恢复与隔离利用语义算法 |
| LSN | Log Sequence Number | 日志序列号 |
| 2PC | Two-Phase Commit | 两阶段提交 |
| 3PC | Three-Phase Commit | 三阶段提交 |
| TCC | Try-Confirm-Cancel | 尝试-确认-取消 |
| SAGA | Saga Pattern | 长事务补偿模式 |
| XA | eXtended Architecture | X/Open 分布式事务标准 |
| TSO | Timestamp Oracle | 时间戳分配器 |
| HLC | Hybrid Logical Clock | 混合逻辑时钟 |
| SSI | Serializable Snapshot Isolation | 可串行化快照隔离 |
| SI | Snapshot Isolation | 快照隔离 |
| CLR | Compensation Log Record | 补偿日志记录 |

## 附录 B: 参考文献

1. **Percolator**: Peng, D., & Dabek, F. (2010). *Large-scale Incremental Processing Using Distributed Transactions and Notifications*. OSDI 2010.
2. **Spanner**: Corbett, J. C., et al. (2012). *Spanner: Google's Globally-Distributed Database*. OSDI 2012.
3. **Calvin**: Thomson, A., et al. (2012). *Calvin: Fast Distributed Transactions for Partitioned Database Systems*. SIGMOD 2012.
4. **ARIES**: Mohan, C., et al. (1992). *ARIES: A Transaction Recovery Method Supporting Fine-Granularity Locking and Partial Rollbacks*. ACM TODS.
5. **CockroachDB**: *CockroachDB Architecture Documentation*. https://www.cockroachlabs.com/docs/
6. **TiDB/TiKV**: *TiKV Transaction Model*. https://tikv.org/docs/
7. **MySQL InnoDB**: *InnoDB Architecture*. https://dev.mysql.com/doc/refman/8.0/en/innodb-architecture.html
8. **PostgreSQL**: *PostgreSQL Documentation - Concurrency Control*. https://www.postgresql.org/docs/
9. Abadi, D. (2017). *Distributed Consistency at Scale: Spanner vs Calvin*. DBMS Musings.
10. *Distributed Transactions (RocketMQ)*. https://rocketmq.apache.org/docs/transactional-message/

